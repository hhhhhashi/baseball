<h1>募集要項詳細ページ</h1>

<div>
  <p>画像</p>
  <%= image_tag @recruit.get_image %>

  <p>タイトル</p>
  <%= @recruit.title %>

  <p>チーム名</p>
  <%= @recruit.team %>

  <p>場所</p>
  <%= @recruit.place %>

  <p>日時</p>
  <%= @recruit.day_and_time %>

  <p>チームレベル(1~10段階)</p>
  <%= @recruit.team_level %>

  <p>本文</p>
  <%= @recruit.content %>

  <p>代表者電話番号</p>
  <%= @recruit.member.telephone_number %>

  <p>代表者メールアドレス</p>
  <%= @recruit.member.email %>

  <p>代表者</p>
  <%= link_to admin_member_path(@recruit.member_id) do %>
  <%= @recruit.member.last_name + @recruit.member.first_name %>
  <% end %>

  <div>
    <p>コメント件数：<%= @recruit.post_comments.count %></p>
    <% @recruit.post_comments.each do |post_comment| %>
      <p><%= image_tag post_comment.member.get_profile_image %></p>

      <%= link_to member_detail_path(post_comment.member.id) do %>
      <p><%= post_comment.member.last_name + post_comment.member.first_name %></p>
      <% end %>

      <%= post_comment.created_at.strftime('%Y/%m/%d') %><%= post_comment.comment %>

      <%= link_to "削除", admin_recruit_post_comment_path(post_comment.recruit, post_comment), method: :delete %>

    <% end %>
  </div>

  <%= link_to "削除する", admin_recruit_path, method: :delete ,class: "btn btn-danger" %>
</div>